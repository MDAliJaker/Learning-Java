public class ShortHandIfElse {
    public static void main(String[] args) {
        int a=5, b=10;

        String statement = (a>b) ? "a is greater than b":"a is samller than b";
        // in Short-Hand If-else if the condition true the first will be the out put else the second one.
        System.out.println(statement);

        System.out.println((a<20) ? "a is less than 20":"a is more than 20");
    }
}
